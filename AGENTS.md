# Shareblog workspace

This repository contains the public, self-hosted Shareblog server and public-site renderer.

The private companion iOS app and share extension are available in the local checkout at:

`/Users/adrian/Library/Mobile Documents/com~apple~CloudDocs/Projects/shareblog-ios`

When work changes the app/server API contract or requires an iOS editor, inspect that repository's `AGENTS.md`, `product-spec.md`, and `ios/ARCHITECTURE.md`, and coordinate the changes across both repositories. Preserve backward compatibility with older installed clients where possible.
